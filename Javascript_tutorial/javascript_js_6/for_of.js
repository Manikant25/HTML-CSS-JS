// array
const students = ['John', 'Sara', 'Jack'];

// using for...of
for ( let element of students ) {

    // display the values
    console.log(element);
}

// string
const string = 'code';

// using for...of loop
for (let i of string) {
    console.log(i);
}